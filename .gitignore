node_modules/
.env
pnpm-lock.yaml
request.http
.log

### macOS ###
# General
.DS_Store


coverage

     // lintfix ejecuta eslint con los cambios en el codigo, corrige errores 
       // lint ejecuta eslint sin hacer cambios en el codigo

en pinologgerwrapper.ts
       //pino usa valores numericos para representar estos niveles: fatal: 60,
//error: 50 ,warn: 40,info: 30,debug: 20,trace: 10 // 

cuando se exporta default en el otro archivo no se importa con las llaves 

se ejecuta el linit fix, cuando ya no hay errores de tipeo, se hacen los test, hay una libreria que se llama 
ts-jest-codemods que analiza el codigo que tengo y genera test , se instala jscodeshift y --save-dev ts-jest-codemods

los metodos de pino en pinologgerwrapper.ts
       //pino usa valores numericos para representar estos niveles: fatal: 60,
//error: 50 ,warn: 40,info: 30,debug: 20,trace: 10 // para poder intercambiarlos con el servidor express del ale. deben ser los mismos metodos del wrapper , entonces, le pongo los de logegrwrapper al pinologger.ts, tengo que hacer el archivo winstonlogger.ts para hacer switch , fijarse que tenga los mismos metodos 

se hace un archivo server en express y otro en koa, en el archivo app.ts se hace el switch 

hay 3 concetos:
wrapper logger y server
el wrapper del servidor  hace de puente entre el logger y el server 
en cada servidor hay un middleware donde se cnfigura ese puente 
